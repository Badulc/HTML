// USE STRICT and  CONSOLE

/*
use strict:
forces us to write 'secure' JavaScript
stops us from using protected words, accidentally making global variables etc...
*/

`use strict`

console.log(`hello world`);

console.info(`this is info`);

console.warn(`this is a warning`);

console.error(`this is an error nessage`);

//styling the console

console.log(`%c` + `red text` + `%c` + `blue text`, `color: red;`, `color: blue;`);

